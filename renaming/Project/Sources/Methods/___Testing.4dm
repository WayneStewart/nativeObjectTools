//%attributes = {"invisible":true,"shared":false}
//var $methodsFolder_t; $msg_t; $DQ_t : Text


//$projectPath_t:=Replace string(Get 4D folder(Database folder); "renaming"; ".github")  // This swaps the location to the project folder on native object tools

//ARRAY TEXT($allFiles_at; 0)
//ARRAY TEXT($allFolders_at; 0)

//FOLDER LIST($projectPath_t; $allFolders_at)
//DOCUMENT LIST($projectPath_t; $allFiles_at)

//// .github 


ALERT(String(Is date))