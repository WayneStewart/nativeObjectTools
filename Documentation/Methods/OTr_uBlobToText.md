<!--OTr_uBlobToText ($theBlob_blob : Blob) -> TexttheBlob : Blob : ➡️ : The BLOB to encodeResult : Text : ⬅️ : Base64-encoded representation-->## OTr\_uBlobToTextOTr\_uBlobToText ($theBlob\_blob : Blob) --> TextBase64-encodes a BLOB to a plain Text string for inline storage in an OTr Object property on 4D versions prior to v19 R2 (that is, where Storage.OTr.nativeBlobInObject is False). Returns empty text if the BLOB is empty. No sentinel prefix is produced.Attributes: Private, Invisible, Preemptive indifferent| Parameter | Type |  | Description || --- | --- | --- | --- ||theBlob|Blob|➡️|The BLOB to encode||Result|Text|⬅️|Base64-encoded representation|