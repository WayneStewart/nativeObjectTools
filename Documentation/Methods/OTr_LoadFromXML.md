<!--OTr_LoadFromXML (inXML) -> LongintinXML : Text : ➡️ : XML string to parseResult : Integer : ⬅️ : New OTr handle, or 0 if parsing failed-->## OTr\_LoadFromXMLOTr\_LoadFromXML (inXML) --> LongintParses an XML string (written by OTr\_SaveToXML) using DOM and loads the reconstructed object into a new OTr handle.Returns 0 if inXML is empty or cannot be parsed. Delegates all reconstruction to OTr\_zXMLReadObject.See OTr\_SaveToXML for a description of the XML schema. Shadow-type keys present in the XML are always restored, regardless of the current OTr\_IncludeShadowKey setting, so that round-trip fidelity is preserved for files that were written with shadow keys included.Attributes: Shared, Invisible, Preemptive indifferent| Parameter | Type |  | Description || --- | --- | --- | --- ||inXML|Text|➡️|XML string to parse||Result|Integer|⬅️|New OTr handle, or 0 if parsing failed|