<!--____Test_InsertDelete Does not require any parameters -->## \_\_\_\_Test\_InsertDelete\_\_\_\_Test\_InsertDelete // Compares two approaches for element insert/delete on object-based arrays using only native 4D commands (no OTr API). Arrays mirror OTr storage: keys "0" through "numElements" (element 0 = pre-selection slot, always empty). Approach A1: Direct key-shifting within the object.Approach A2: Extract to ARRAY TEXT, use native INSERT IN ARRAY / DELETE FROM ARRAY, rebuild. Each result shows two lines: Logical: iterated 1..numElements  (always correct) JSON:    JSON Stringify            (reveals key order) // Tests: T1. Delete Wednesday  (days,   pos 3) T2. Delete July       (months, pos 7) T3. Insert "Koala"  before Wednesday  (days,   pos 3) T4. Insert "Wombat" before September  (months, pos 9) // /| Parameters || --- || Does not require any parameters |