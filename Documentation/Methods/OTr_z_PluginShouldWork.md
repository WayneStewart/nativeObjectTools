<!--OTr_z_PluginShouldWork -> BooleanResult : Boolean : ⬅️ : Returns true if the plugin is a) present and b) the OS supports it     or the installed plugin version is known to support the current OS.-->## OTr\_z\_PluginShouldWorkOTr\_z\_PluginShouldWork --> BooleanDetermines if the plugin should work based on plugin presence, OS, and ObjectTools plugin version compatibility.Attributes: Private, Invisible, Preemptive indifferent| Parameter | Type |  | Description || --- | --- | --- | --- ||Result|Boolean|⬅️|Returns true if the plugin is a) present and b) the OS supports it     or the installed plugin version is known to support the current OS.|