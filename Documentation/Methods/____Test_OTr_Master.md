<!--____Test_OTr_Master Does not require any parameters -->## \_\_\_\_Test\_OTr\_Master\_\_\_\_Test\_OTr\_Master // Single-call master test runner for the entire OTr test suite.Executes all unit-test phases (1, 1.5, 2, 3, 4, 5, 6, 16, 8) inline, accumulating results in a six-column TAB-delimited table: Num | Phase | Test Name | Expected | Actual | Pass/Fail // Then calls each side-by-side controller (10, 10a, 10b, 10c, 15) with suppressAlert=True so they write their own Logs files silently, and appends a summary reference line for each. The master document is prepended with a header row containing: Platform (Mac/Windows) | 4D version string | Date/Time of run // Output written as UTF-8 to the 4D Logs folder: \_\_\_\_Test\_OTr\_Master-YYYY-MM-DD-HH-MM-SS.txt // Attributes: Shared, Preemptive indifferent| Parameters || --- || Does not require any parameters |