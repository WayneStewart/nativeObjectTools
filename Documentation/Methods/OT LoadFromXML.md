<!--OT LoadFromXML (inXML) -> LongintinXML : Text : ➡️ : XML string to parseResult : Integer : ⬅️ : New OTr handle, or 0 if parsing failed-->## OT LoadFromXMLOT LoadFromXML (inXML) --> LongintParses an XML string (written by OT SaveToXML) using DOM and loads the reconstructed object into a new OTr handle.Returns 0 if inXML is empty or cannot be parsed. Delegates all reconstruction to OTr\_z\_XMLReadObject.See OT SaveToXML for a description of the XML schema. Shadow-type keys present in the XML are always restored, regardless of the current OT IncludeShadowKey setting, so that round-trip fidelity is preserved for files that were written with shadow keys included.Attributes: Shared, Invisible, Preemptive indifferent| Parameter | Type |  | Description || --- | --- | --- | --- ||inXML|Text|➡️|XML string to parse||Result|Integer|⬅️|New OTr handle, or 0 if parsing failed|