<!--i; $newHandle_i : Integer Does not require any parameters -->## \_\_\_\_\_Import\_OT\_Blobvar $Options\_i; $newHandle\_i : Integer var $FolderPath\_t; $DocumentName\_t; $DocumentPath\_t : Text var $import\_blob : BlobARRAY TEXT($paths\_at; 0)look up file $Options\_i:=Allow alias files+Package open+Use sheet window $FolderPath\_t:=System folder(Desktop) $DocumentName\_t:=Select document($FolderPath\_t; ".blob"; "Select a Blob"; $Options\_i; $paths\_at)Import the blob If ($DocumentName\_t#"") 	Get the path 	$DocumentPath\_t:=String($paths\_at{1})	DOCUMENT TO BLOB($DocumentPath\_t; $import\_blob)	$newHandle\_i:=OTr\_BLOBToObject($import\_blob)	If ($newHandle\_i=0) 		ALERT("Something went wrong")	Else 		OTr\_z\_ObjectViewer	End ifEnd if| Parameters || --- || Does not require any parameters |