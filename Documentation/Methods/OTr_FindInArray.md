<!--OTr_FindInArray (inObject; inTag; inValue {; inStart}) -> LongintinObject : Integer : ➡️ : OTr handleinTag : Text    : ➡️ : Tag path to the array iteminValue : Text    : ➡️ : Value to search for (as text)inStart : Integer : ➡️ : Starting index, 1-based; default 1 (inStart)Result : Integer : ⬅️ : 1-based index of match, or -1-->## OTr\_FindInArrayOTr\_FindInArray (inObject; inTag; inValue {; inStart}) --> LongintSearches an array stored in an OTr object for a value matching inValue. The value is passed as text and converted to the stored array type before searching. For Boolean arrays, "true" or "1" are treated as True; any other value is treated as False. Not supported for Pointer, BLOB, or Picture arrays.If the array type does not support searching, or if the handle or tag are invalid, an error is generated, OK is set to zero, and -1 is returned. The result matches the native Find in array command:the 1-based index of the first matching element, or -1 if the value is not found in the array.Attributes: Shared, Invisible, Preemptive indifferent| Parameter | Type |  | Description || --- | --- | --- | --- ||inObject|Integer|➡️|OTr handle||inTag|Text   |➡️|Tag path to the array item||inValue|Text   |➡️|Value to search for (as text)||inStart|Integer|➡️|Starting index, 1-based; default 1 (inStart)||Result|Integer|⬅️|1-based index of match, or -1|