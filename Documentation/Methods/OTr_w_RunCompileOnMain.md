<!--OTr_w_RunCompileOnMain (signal) Does not require any parameters -->## OTr\_w\_RunCompileOnMainOTr\_w\_RunCompileOnMain (signal) // Runs the non-thread-safe compile/syntax check from the main process, then returns a JSON string through the supplied signal.#DECLARE($signal\_o : Object)var $compile\_o; $summary\_o : Object var $resultJSON\_t : Text$compile\_o:=OTr\_w\_compileProject $summary\_o:=OTr\_w\_CompileResultForJSON($compile\_o) $resultJSON\_t:=JSON Stringify($summary\_o)Use ($signal\_o) 	$signal\_o.resultJSON:=$resultJSON\_t End use$signal\_o.trigger()| Parameters || --- || Does not require any parameters |